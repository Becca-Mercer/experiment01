name: build with composite action
on: 
  pull_request:
    types: [open]
permissions:
  pull_requests: write 

jobs:
  build:
    run-on: ubuntu-latest
    steps:
      - name: Use My composite action to build 
        uses: ./.github/actions/my-composite-action
        with: 
          node-version: 18
